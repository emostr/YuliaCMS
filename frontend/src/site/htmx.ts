// The htmx bundle a published site loads.
//
// Importing the library is enough: htmx installs itself on the document and
// picks up hx- attributes as they appear. Note that in htmx 4 extensions are
// activated by being loaded, not by an hx-ext attribute.
import 'htmx.org';
